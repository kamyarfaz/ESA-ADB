# Earlier saved results

This directory preserves the earlier `mission1_reconstruction_ae_sweep_optimized`
experiment output. The active research code defaults to `../results_longrun/`.
A matching run timestamp does not establish identical contents. Files here were
not deleted or deduplicated during organization.
