# Weights & Biases local records

Offline run directories and their `latest-run`/debug symlinks are preserved together.
The research logging adapter is `../esa_thesis/tracking.py`. These records are
local artifacts and excluded from Git. Uploading them is a separate action.
